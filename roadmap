Flag -> Commands [~]
Banner -> Stringa [V]
Usage -> la libreria Flag ha una funzionalità apposta: Usage è di tipo func, se la cambio cambia l'help
```
func init(){
	flag.Usage = func() {
	///tuo codice
	flag.PrintDefaults()
}
```
NewDecoder -> Unmarshal: open, readall -> []byte -> unmarshall
Open,Controllo Errore, !!Defer(file.Close)!!

La funzione init viene eseguita quando il pack è importato o immediatamente se è il main